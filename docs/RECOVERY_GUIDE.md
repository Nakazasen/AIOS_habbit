# Recovery Guide
If private data is generated, keep it local and do not stage it. If it is staged, run `git rm --cached <file>` or `git reset <file>` and add/update `.gitignore`. Do not delete local private data unless explicitly required.
