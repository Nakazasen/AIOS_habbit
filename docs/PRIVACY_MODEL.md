# Privacy Model
Public repo contents are code, docs, schemas, templates, and synthetic samples only.
Private runtime data is local and gitignored, including evidence JSONL, memory JSONL, candidate extraction output, generated export packs, and final local handover/audit reports.
Discovery is metadata-only. Extraction creates candidates, never verified truth. Verified memory needs evidence. Export packs must be audited before use.
