# Phase Gate Process
`aios-habit phase validate --phase N` performs real deliverable checks. A phase fails when required docs, modules, validation behavior, audit fixtures, or handover content are missing.
