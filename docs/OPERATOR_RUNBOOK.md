# Operator Runbook
1. Run tests and audit.
2. Run discovery with `--dry-run` first.
3. Add evidence before verified memory.
4. Keep unsupported claims as UNKNOWN or needs_evidence.
5. Build profiles only from verified/export-allowed memory.
6. Audit before export or push.
