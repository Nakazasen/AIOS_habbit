# 安装方案A运行成本

| 项目 | 月费用 | 说明 |
| --- | --- | --- |
| 人工 | 12000 | 两班 |
| 电费 | 4500 | 旧设备 |
| 维护 | 1800 | 定期 |

方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。方案A的运行成本主要来自人工。与方案B对比时必须同时计算电费和维护费用。
