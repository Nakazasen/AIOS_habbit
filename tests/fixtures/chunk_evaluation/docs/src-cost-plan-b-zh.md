# 安装方案B运行成本

| 项目 | 月费用 | 说明 |
| --- | --- | --- |
| 人工 | 8000 | 自动班 |
| 电费 | 6200 | 新电机 |
| 维护 | 3100 | 外包 |

方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。方案B人工较低，但电费和维护费用更高。对比两种安装方案的运行成本和维护费用时不可只看人工。
