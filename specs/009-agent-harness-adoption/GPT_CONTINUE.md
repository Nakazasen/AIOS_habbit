# Prompt tiếp cho GPT / Gemini — Goal 009

Dán nguyên khối dưới. Không đọc thẻ cổng cũ chữ `BLOCKED`.

```text
Goal 009 trong D:\Sandbox\AIOS_habbit. Nhánh gate1-local-case-sqlite.

ĐỌC: specs/009-agent-harness-adoption/GPT_CONTINUE.md, spec.md mục 1.1 «Cấm over-engineer», tasks.md, contracts/agent-factory-error-report-v1.md.

BẮT ĐẦU T011. Bỏ qua T006–T010. Không tạo opencode_runtime_adapter.py. Không probe Cline. Không fork. Không ghi Goal hay G1 = BLOCKED.

G1 = PARTIAL (chỉ hoãn sửa mã US3). Goal ACTIVE. US1/US2 không cần OpenCode.

Làm T011→T015 (file báo cáo lỗi dùng được, tự «Đã xong», extractors/Mermaid hiện có) rồi T016→T019 (rà soát Guideline). UI tiếng Việt, không nút duyệt, không hiện schema.

Python 3.11: uv run --no-sync --group dev. Chỉ fixture tests/fixtures/agent_harness/. Không commit trừ khi owner bảo. Không giả PASS. Hai cách cùng đạt: ít code hơn.
```
