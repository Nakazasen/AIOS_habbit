# Execution Plan: Excaliflow Skill Upgrade

## Objective
Upgrade `C:\Users\Admin\.gemini\config\skills\excaliflow` to include:
1. **R1**: Zoom and Pan feature for HTML diagram viewer generated by `excaliflow`.
2. **R2**: Collapsible sidebar with toggle button to expand diagram viewing area.
3. **R3**: Package upgraded skill into `C:\Users\Admin\Downloads\excaliflow-skill-v2.zip`.
4. **R4**: Auto-detect and ingest `graphify-out/graph.json` or `/understand` data to build rich Mermaid architecture/call graph diagrams with fallback to AST scan.

## Steps
1. **Survey (Phase 0)**:
   - Spawn Explorers to inspect current files in `C:\Users\Admin\.gemini\config\skills\excaliflow` (specifically `generate_diagram.py`, HTML template structure, JS libraries, packaging needs, graphify-out format, and Playwright verification environment).
2. **Architecture & Decomposition (Phase 1)**:
   - Synthesize findings into `PROJECT.md`. Define file changes, zoom/pan library selection, collapsible sidebar transitions, graph.json parser logic, and zip structure.
3. **Implementation (Phase 2 - Milestone 1: Core Engine & Graphify)**:
   - Worker implements Graphify knowledge graph ingestion (`graphify-out/graph.json`) with AST fallback in `generate_diagram.py`.
4. **Implementation (Phase 2 - Milestone 2: UI Features)**:
   - Worker implements zoom/pan and collapsible sidebar in `generate_diagram.py` HTML template.
5. **Verification & Packaging (Phase 3 - Milestone 3)**:
   - Worker creates sample diagram (testing both graphify mode and AST fallback) and executes Playwright automated verification script testing zoom, pan, and sidebar toggle.
   - Worker packages the entire skill into `C:\Users\Admin\Downloads\excaliflow-skill-v2.zip` and verifies its contents.
6. **Quality & Integrity Assurance (Phase 4)**:
   - 2 Reviewers review code & functionality.
   - 2 Challengers adversarially stress-test UI and edge cases.
   - 1 Forensic Auditor verifies integrity and absence of hardcoded dummy facades.
7. **Delivery & Reporting (Phase 5)**:
   - Save AgentMemory checkpoint.
   - Deliver comprehensive final handoff to parent.
