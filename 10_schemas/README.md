# 10_schemas — Phase 0 vault (không phải lối vào)

Status: `RETIRED` for first-read.

Schema JSON ở đây thuộc memory vault Giai đoạn 0. Contract runtime hiện tại: `docs/contracts/`. Lối vào: `AGENTS.md`.
